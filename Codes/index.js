const convention = require('./codeConvention')
const status = require('./statusCode')
const error = require('./errorCode')
const success = require('./successCode')

module.exports = {
    convention,
    status,
    error,
    success
}